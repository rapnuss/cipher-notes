services:
  app:
    container_name: ciphernotes_app
    build:
      context: ..
      dockerfile: dockerimage/Dockerfile
    env_file:
      - .env
    ports:
      - "13064:80"
    depends_on:
      db:
        condition: service_healthy
      minio:
        condition: service_healthy
      minio-setup:
        condition: service_completed_successfully
    networks:
      - backend
    restart: unless-stopped

  db:
    image: postgres:16
    container_name: ciphernotes_db
    environment:
      POSTGRES_USER: notes
      POSTGRES_PASSWORD: notes
      POSTGRES_DB: notes
      POSTGRES_INITDB_ARGS: "--locale=C.UTF-8 --encoding=UTF8"
      LANG: C.UTF-8
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U notes"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 5s
    volumes:
      - db_data:/var/lib/postgresql/data
    networks:
      - backend
    restart: unless-stopped

  minio:
    image: pgsty/minio:RELEASE.2026-04-17T00-00-00Z
    container_name: ciphernotes_minio
    env_file:
      - .env
    command: server /data --console-address ":9001"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:9000/minio/health/ready || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data
    networks:
      - backend
    restart: unless-stopped

  minio-setup:
    image: pgsty/mc:RELEASE.2026-04-17T00-00-00Z
    container_name: ciphernotes_minio_setup
    env_file:
      - .env
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: ["/bin/sh", "-c"]
    command: >-
      "mc alias set local http://minio:9000 ${MINIO_ROOT_USER} ${MINIO_ROOT_PASSWORD} &&
      (mc ls local/ciphernotes || mc mb -p local/ciphernotes || true)"
    networks:
      - backend
    restart: "no"

networks:
  backend:
    name: ciphernotes_backend

volumes:
  db_data:
  minio_data:
